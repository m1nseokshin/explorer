<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# EXPLORER — 프로젝트 규칙

## 절대 건드리기 전에 읽을 것

- **`DESIGN.md`** — 디자인 시스템의 단일 진실 공급원. 새 화면을 만들기 전에
  "Iteration Guide" 절의 여섯 질문을 먼저 통과시킬 것.
- **`lib/sky.ts`** — 좌표 변환의 전부. `Rotation_EQJ_HOR`의 `rot[i]`는 행이
  아니라 **열**이다. 전치하면 하늘이 통째로 뒤집히는데 화면은 그럴듯해 보인다.
- **`lib/gestures.ts`** — 랜드마크 → 제스처. 순수 함수만 유지할 것. 여기가
  브라우저 없이 검증 가능한 유일한 지점이다.

## 바꾸면 반드시 `npm run verify`

314개 검사를 브라우저도 카메라도 없이 돈다. 아래 중 하나라도 건드렸으면 통과시킬 것.

- `lib/sky.ts`, `scripts/build-star-data.mjs`, `public/data/*` → 천문 290개
- `lib/gestures.ts`의 임계값이나 지표 → 제스처 24개
  (`CURVE=1`을 붙이면 curl→openness 곡선을 직접 볼 수 있다)

## 조용히 틀리기 쉬운 지점

1. **RA 단위** — `astronomy-engine`의 `Equator()`와 `Constellation()`은 적경을
   '시간' 단위로 주고받는다. ×15를 빠뜨리면 하늘이 15배 회전한다.
2. **달의 지평시차** — `Equator()`에 `observer`를 넘기지 않으면 지심 좌표가 나오고,
   렌더된 달이 실제 달에서 달 지름 두 개만큼 빗나간다.
3. **하늘 그룹은 `quaternion`으로만** — `matrixAutoUpdate = false` + 수동 `matrix`는
   r3f 리컨실러가 되돌려 놓아 하늘이 항등행렬로 남는다. 증상은 "북쪽을 봤는데
   남극 별자리가 보임"이다.
4. **첫 프레임도 맞아야 한다** — 프레임 루프가 스로틀링되는 환경(백그라운드 탭,
   저전력)에서는 `useFrame`이 안 돈다. 하늘 변환은 `useLayoutEffect`로 즉시 1회
   적용한 뒤 `useFrame`으로 갱신한다.
5. **손 척도는 손목→중지 MCP** — 손가락 길이를 척도로 쓰면 주먹을 쥘 때 척도가
   같이 줄어 판정이 무너진다. `handScale()`을 우회하지 말 것.
6. **핀치 판정이 openness보다 우선** — 순서를 뒤집으면 핀치 중에 접힌 검지·엄지 때문에
   '펼침'으로 오독된다.
7. **인식용 `<video>`를 `display:none`으로 숨기지 말 것** — 일부 브라우저가 디코딩을
   멈춰 손 인식이 통째로 죽는다. 1px 투명으로 숨긴다.
8. **손 조작 위에 또 슬러핑하지 말 것** — `HandControls`가 이미 속도 저역통과를
   거친다. `SkyRig`에서 다시 보간하면 손을 멈춰도 하늘이 계속 밀리는 고무줄이 된다.
9. **`fetch()`도 `asset()`을 통과해야** GitHub Pages basePath에서 동작한다.
   `public/data/*.bin`, `public/models/*.task`, `public/mediapipe/wasm` 전부 해당된다.
10. **전처리 스크립트와 `radecToVec3`는 같은 공식** — 어긋나면 별자리선이 별에서
    미세하게 떨어져 나가고, 원인을 찾기 매우 어렵다.
11. **타입 티어는 `@layer components` 안에 있어야 한다** — Tailwind는 파일 맨 위
    `@import "tailwindcss"`에서 유틸리티를 만든다. `.type-display-lg` 같은 클래스를
    레이어 없이 그 아래에 두면 나중 선언이 이겨서 `text-xl`이 통째로 무시된다.
    `type-display-lg text-sm`이라고 써도 28px로 나오는데, 화면은 그럴듯해 보인다.
12. **이펙트 의존성에 인라인 콜백을 넣지 말 것** — `onPick={(a,b) => ...}` 같은 걸
    이벤트 리스너 이펙트의 의존성에 두면 부모가 리렌더될 때마다 리스너가 다시
    붙는다. 드래그 중이면 그 순간 포인터 상태가 초기화돼 이동량을 잃는다.
    증상은 "잡고 끌어도 조금만 움직임"이고 원인은 좌표 계산이 아니다.
    바뀌는 값은 ref에 모으고 이펙트는 마운트 시 한 번만 만든다.
13. **지도 경도는 매 프레임 되감을 것** — 무한 스크롤이라 경도가 무한정 커진다.
    현재 뷰와 목표를 '같은 양'만큼 [-180,180)으로 옮겨야 보간이 튀지 않는다.
    안 하면 몇 바퀴 뒤에 대륙이 사라진다(사본 범위를 벗어나서).
14. **구면 폴리곤 면적은 극을 감싸는지 확인할 것** — 경도가 한 바퀴 돌면
    사다리꼴 적분이 '여집합' 면적을 준다. 작은곰자리·팔분의자리만 20,000
    제곱도로 나오면 이 보정이 빠진 것이다.
15. **뱀자리는 두 조각이다** — 원본 데이터에 Ser이 두 번 나온다. 병합하지 않으면
    별자리가 89개가 되고 면적 순위가 통째로 밀린다.
16. **캡처 오버레이는 z-0** — 이보다 높으면 별자리 라벨 버튼을 덮어 클릭이
    통째로 먹힌다. 하늘은 여전히 드래그되므로 겉보기엔 멀쩡해 보인다.
17. **조준 글로우는 슬롯 두 개로 교차 페이드** — 지오메트리 하나를 바꿔 끼우면
    조준이 옮겨가는 순간 이전 별자리가 '툭' 꺼진다. `ConstellationLines`의
    `AimGlow`는 슬롯을 번갈아 쓰고 불투명도를 `useFrame`에서 머티리얼에 직접
    쓴다. Motion이나 state로 하면 초당 60회 3D 트리 재조정이 걸린다.
18. **같은 이름을 두 번 적지 말 것** — 조준한 별자리는 라벨이 이미 밝아진다.
    레티클에도 이름을 적으면 화면에 같은 단어가 둘이다. 라벨 레이어가 꺼졌을
    때만 레티클이 대신 알려준다.
19. **지면은 깊이가 아니라 덧칠로 가린다** — 하늘 요소가 전부 `depthTest=false`라
    지면 메시를 놔도 깊이로는 아무것도 안 가려진다. `GroundDome`의 renderOrder(5)를
    별·선·행성(-1~4)보다 뒤에 두어 덮고, 지평선 선만 그보다 더 뒤(6)에 그려
    경계가 살아남는다. 자오선은 0으로 남겨 지하 구간이 저절로 잘리게 한다.
20. **하늘 밝기는 수평선에서 최대다** — 밤에는 낮게 깔린 광해 때문에 위로
    갈수록 어두워진다(`skyGlow`). 이걸 뒤집으면 천정이 빛나는 이상한 하늘이 된다.
21. **물은 땅이 아니다** — 잔잔한 물은 스치는 각도에서 거의 완전한 거울이라,
    수평선 바로 아래가 바로 위의 하늘만큼 밝아야 물로 읽힌다. '땅은 하늘보다
    어둡다'를 그대로 적용해 어둡게 깔면 검은 땅이 된다. 밝기는 반사되는 하늘
    (`skyGlow`)에 슐릭 반사율(`fresnel`)을 곱해서 얻고, 물결은 밝기를 곱해
    흔드는 게 아니라 **반사각 자체**를 흔들어야 질감이 된다.
22. **달빛 길은 광선이 아니라 조각들이다** — 물결이 상을 토막내지 않으면 즉시
    탐조등처럼 보인다. 대부분은 어둡고 가끔만 밝아야 한다.
23. **물결은 옥타브마다 다른 지점에서 눕힌다** — 한꺼번에 눕히면 수평선 띠가
    통째로 매끈해져 안개 막대가 된다. 반대로 안 눕히면 파장이 1픽셀보다
    작아져 그 띠가 지글거린다.
24. **윤슬은 하늘 그룹 '안'에 있어야 한다** — `SeaGlitter`는 modelMatrix가 곧
    하늘 회전이어야 월드 y를 뒤집는 것만으로 정확한 거울상이 된다. 그룹 밖에
    두면 하늘이 돌아도 반사는 가만히 있어서 즉시 가짜로 보인다. 반사 세기는
    눈대중이 아니라 슐릭 근사다 — 임의로 정하면 하늘이 다 비쳐 거울 바닥이 된다.
25. **물속은 투명해지는 게 아니라 가려지는 것이다** — 돌고래를 알파로 페이드
    시키면 유령처럼 스르륵 사라져서 즉시 인위적으로 보인다. 눈이 수면 높이에
    있으므로 월드 y=0이 곧 수면이고, 프래그먼트에서 그 아래를 잘라내야 물속으로
    파고드는 것처럼 보인다. 그래서 스프라이트가 아니라 메시다 — 스프라이트는
    프래그먼트에서 월드 좌표를 알 수 없어 자를 수가 없다.
26. **먼 섬은 지평선 선보다 뒤에 그린다** — `SeaIsland`(renderOrder 7)가
    지평선 선(6)보다 앞이면 수평선이 섬을 가로질러 섬이 반투명해 보인다.
    실제로는 섬이 수평선을 가린다.
27. **`useFrame`이 쓰는 값을 JSX에 적지 말 것** — r3f는 리렌더마다 JSX에 적힌
    prop을 되돌린다. `SkyCanvas`는 조준 별자리 같은 값 때문에 초당 몇 번씩
    리렌더되므로, `visible`·`map`·`opacity`를 JSX에 두면 `useFrame`이 켜 둔 값이
    곧바로 꺼진다. 이 한 가지로 버그가 둘 났다 — 돌고래가 영영 안 보였고
    (`visible`), 별자리 조준 글로우의 크로스페이드가 매번 '툭' 끊겼다
    (`opacity`). 객체·머티리얼을 직접 만들어 `<primitive>`나 `material={}`로
    넘기면 r3f가 손대지 않는다.
    같은 이유로 **`uniforms` useMemo가 다시 만들어질 때**(야간모드 토글 등)
    이펙트로 써 둔 유니폼도 초기화된다 — 의존성에 `uniforms`를 넣을 것.
28. **정적인 값을 셰이더에서 풀지 말 것** — 은하수의 먼지 얼룩과 은하 중심
    거리는 점마다 고정이다. 정점 셰이더에 두면 48,018개 × 3옥타브 × sin 8회를
    매 프레임 다시 계산하는데 결과는 매번 같다. 마운트 때 구워서 속성으로 올린다.
29. **지도 투영은 종횡비를 반영해야 한다** — 경도·위도의 '픽셀당 도'가 같아야
    대륙이 안 늘어난다. `latSpan`을 180/zoom으로 못 박으면 정사각 캔버스에서
    지구가 세로로 2배 늘어나는데, 화면은 그럴듯해 보이고 대륙 모양만 조용히
    틀린다. 하한 배율도 종횡비가 정한다 — 정사각이면 2(위도 전체 + 경도 180°).
30. **저장된 설정은 지연 초기화로 읽을 것** — 이펙트에서 복원하면 값을 저장하는
    이펙트가 먼저 돌면서 기본값으로 덮어써 설정이 조용히 유실된다.

## 데이터 정확성 규약

**이름만으로 별을 맞추지 말 것.** 고유명은 하늘에서 유일하지 않다 — `Alnair`는
두루미자리 α와 켄타우루스자리 ζ 둘 다에 붙어 있고 `Markab`·`Menkar`도 마찬가지다.
별자리까지 함께 봐야 엉뚱한 별에 유래나 항법별 표시가 붙지 않는다. 카탈로그는
카시오페이아 α를 IAU 표기 `Schedar`가 아니라 `Shedar`로 적는다 — 표기가 어긋나면
화면에는 아무 일도 안 일어나서 눈으로는 절대 못 잡는다. `verify-sky.mjs`의 H11이
이걸 잠근다.

**모르는 숫자는 적지 않는다.** 색온도 식(Ballesteros)은 태양에서 6K 안쪽으로
맞지만 B−V < −0.1에서는 25,000K를 16,600K로 준다. 그 구간에서는 숫자를 빼고
'2만 K를 넘는 부류'라고만 적는다.

## 카피 규약

주제는 **항해**다. 별자리는 감상거리가 아니라 항법 도구였고, 이 제품은 그 계산을
거꾸로 돌린 것이다 — 항해자는 별을 재서 위치를 얻었고, 여기서는 위치를 받아 별을
그린다. 새 문구를 쓸 때 이 축에서 벗어나지 말 것.

- '조타'가 아니라 **'항해'**로 통일한다.
- 의성어(훽훽 등)를 쓰지 않는다. "좌우로 젓는다"처럼 동작으로 적는다.
- 버튼은 **상태가 아니라 행동**으로 적는다. '권한 거부됨'이 아니라 '내 위치로 찾기' —
  상태 통보는 다음에 뭘 해야 하는지를 알려주지 않는다.

## 라우트

- `/explore` — 실시간 하늘. 시간 조작이 없다.
- `/timelapse` — 같은 하늘에 시간 컨트롤만 얹는다 (`<SkyClient timelapse />`).

한 화면이 둘을 겸하면 '지금 보이는 게 진짜 하늘인가'라는 질문이 늘 따라붙는다.
`/explore`는 들어올 때마다 `resetTime()`으로 실시간에 맞춘다 — 이전 세션에서
시간을 돌려 둔 상태가 남으면 하늘이 틀린 채로 시작한다.

## 제스처 규약

**확대와 축소를 서로 다른 손 모양에 배정하지 말 것.** 어느 쪽이 어느 쪽인지
외워야 하고, 둘 사이를 오가려면 자세를 바꿔야 해서 미세 조정이 불가능해진다.
하나의 축(카메라와의 거리)에 양방향으로 얹는다 — 밀면 다가가고 당기면 물러난다.
되돌리는 것도 같은 축이라 배울 게 없다.

깊이 줌은 **속도 매핑**이지 절대 매핑이 아니다. 손 크기를 배율에 직접 물리면
팔 길이가 곧 배율 범위의 상한이 된다. 기준점(`neutralRef`)은 손이 사라지거나
저을 때마다 지금 위치로 다시 잡는다 — 안 그러면 손을 다시 들 때 배율이 튄다.

## 모션 규약

**손가락이 직접 끈 것에만 스프링을 쓴다.** 시스템이 알아서 정한 변화(레이어 토글, 라벨
페이드)는 duration을 쓴다. 잡지도 않았는데 튕기는 건 소음이다.

Motion(구 Framer Motion)은 **몰입 라우트 전용**이다. 60fps로 도는 경로
(`SkyRig`, `HandControls`, `SkyGroupUpdater`, `HandOverlay`)에는 절대 넣지 말 것 —
그쪽은 React 리렌더를 피하려고 ref로 도는데, Motion을 끼우면 다시 렌더 사이클로
끌려 들어온다.

## 성능 규약

시뮬레이션 상태(시각·자세·하늘 행렬·손 상태)는 전부 **ref**에 둔다. React state에
넣으면 초당 60회 리렌더가 나서 트리 전체가 죽는다. 벤치마크 프로젝트
(`../solarsystem`)의 `simDateRef` / `groupsRef` 규약과 동일하다.

손 인식 결과를 UI에 노출할 때는 10Hz로 스로틀링한다(`HandControls`의 `report`).
